# scripts/deploy.env 로 복사해 값을 채운다(이 example만 커밋, 실제 deploy.env는 gitignore).
#   cp scripts/deploy.env.example scripts/deploy.env
NB_SSH=ubuntu@140.238.0.0.sslip.io   # VM 접속 대상(사용자@호스트)
# NB_SSH_KEY=~/.ssh/oracle_vm        # (선택) SSH 개인키 경로
# NB_REMOTE_DIR=~/number-baseball    # (선택) VM의 리포 경로(기본값)
# NB_BRANCH=main                     # (선택) 배포 브랜치(기본값)
# NB_PM2_NAME=nb-server              # (선택) pm2 프로세스 이름(기본값)
